# Azure CLI - macOS PKG Complete Signing Pipeline
# This pipeline:
# 1. Downloads unsigned PKG from build artifacts
# 2. Extracts all binaries from the PKG
# 3. Signs all binaries (Python, libraries, executables) with ESRP
# 4. Repacks the PKG with signed binaries
# 5. Signs the PKG itself with ESRP
# Output: Fully signed PKG ready for notarization

trigger: none # Manual trigger only

parameters:
  - name: SourceBuildId
    displayName: 'Source Build ID (the build that created the unsigned PKGs)'
    type: string
    default: '282071'
  - name: SourcePipelineName
    displayName: 'Source Pipeline Name'
    type: string
    default: 'naga_macos_build'
  - name: AzureCliVersion
    displayName: 'Azure CLI Version (e.g., 2.0.0)'
    type: string
    default: '2.0.0'
  - name: BundleId
    displayName: 'Bundle ID for signing'
    type: string
    default: 'com.microsoft.azure.cli'
  - name: OfficialBuild
    displayName: 'Official Build (enables ESRP signing)'
    type: boolean
    default: true

variables:
  - group: 'AME ESRP Variable Group'
  
  - name: DOTNET_CLI_TELEMETRY_OPTOUT
    value: 1
  - name: DOTNET_NOLOGO
    value: 1
  - name: NugetSecurityAnalysisWarningLevel
    value: none
  - name: skipNugetSecurityAnalysis
    value: true
  - name: AzureCliVersion
    value: ${{ parameters.AzureCliVersion }}
  - name: SourceBuildId
    value: ${{ parameters.SourceBuildId }}
  - name: SourcePipelineName
    value: ${{ parameters.SourcePipelineName }}
  - name: BundleId
    value: ${{ parameters.BundleId }}

name: sign-all-macos-pkg-$(AzureCliVersion)-$(Build.BuildId)

stages:
- stage: DownloadAndExtract
  displayName: 'Download Unsigned PKG and Extract Binaries'
  jobs:
  - job: DownloadAndExtractPkg
    displayName: 'Download and extract PKG contents'
    pool:
      vmImage: 'macos-14'
    
    steps:
    - checkout: none
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download ARM64 PKG'
      inputs:
        buildType: 'specific'
        project: 'public'
        definition: $(SourcePipelineName)
        buildVersionToDownload: 'specific'
        buildId: $(SourceBuildId)
        artifactName: 'pkg-installer-macos-arm64'
        targetPath: '$(Pipeline.Workspace)/unsigned-pkg'
    
    - bash: |
        set -e
        echo "=== Downloaded PKG Files ==="
        ls -lh $(Pipeline.Workspace)/unsigned-pkg/
        echo ""
        
        # Find the PKG file
        PKG_FILE=$(find $(Pipeline.Workspace)/unsigned-pkg -name "*.pkg" -type f | head -1)
        
        if [ -z "$PKG_FILE" ]; then
          echo "❌ ERROR: No PKG file found!"
          exit 1
        fi
        
        echo "✅ Found PKG: $PKG_FILE"
        echo "PKG size: $(du -h "$PKG_FILE" | cut -f1)"
        
        # Create extraction directory
        EXTRACT_DIR="$(Pipeline.Workspace)/pkg-extracted"
        mkdir -p "$EXTRACT_DIR"
        
        echo ""
        echo "=== Extracting PKG (XAR archive) ==="
        cd "$EXTRACT_DIR"
        xar -xf "$PKG_FILE"
        
        echo ""
        echo "=== PKG Structure ==="
        ls -lh
        
        # Find the component PKG
        COMPONENT_PKG=$(find . -name "*.pkg" -type d | grep -v "^\.$" | head -1)
        
        if [ -z "$COMPONENT_PKG" ]; then
          echo "❌ ERROR: No component PKG found!"
          exit 1
        fi
        
        echo ""
        echo "✅ Found component PKG: $COMPONENT_PKG"
        
        # Extract the Payload (contains actual files)
        echo ""
        echo "=== Extracting Payload ==="
        PAYLOAD_DIR="$(Pipeline.Workspace)/payload"
        mkdir -p "$PAYLOAD_DIR"
        
        cd "$PAYLOAD_DIR"
        cat "$EXTRACT_DIR/$COMPONENT_PKG/Payload" | gunzip -dc | cpio -i
        
        echo ""
        echo "=== Payload extracted to: $PAYLOAD_DIR ==="
        echo "Total size: $(du -sh . | cut -f1)"
        
        # Find all executables and libraries that need signing
        echo ""
        echo "=== Finding files to sign ==="
        
        # Python executables
        find . -type f -name "python*" ! -name "*.pyc" ! -name "*.py" | head -10
        
        # Shared libraries
        echo ""
        echo "Dylibs found:"
        find . -type f -name "*.dylib" | wc -l
        
        echo ""
        echo "SO files found:"
        find . -type f -name "*.so" | wc -l
        
        # az CLI executable
        echo ""
        echo "az executable:"
        find . -type f -name "az" -o -name "azurecli"
        
      displayName: 'Extract PKG and Payload'
    
    - bash: |
        set -e
        echo "=== Creating file list for signing ==="
        
        PAYLOAD_DIR="$(Pipeline.Workspace)/payload"
        FILES_TO_SIGN="$(Pipeline.Workspace)/files-to-sign.txt"
        
        cd "$PAYLOAD_DIR"
        
        # Find all binaries that need signing:
        # 1. Mach-O executables (including Python)
        # 2. Dynamic libraries (.dylib, .so)
        # 3. Frameworks
        
        echo "Scanning for Mach-O binaries..."
        
        # Use 'file' command to identify Mach-O binaries
        find . -type f | while read filepath; do
          filetype=$(file -b "$filepath")
          
          # Check if it's a Mach-O binary
          if echo "$filetype" | grep -q "Mach-O"; then
            echo "$filepath"
          fi
          
          # Also include .dylib and .so files
          if [[ "$filepath" == *.dylib ]] || [[ "$filepath" == *.so ]]; then
            echo "$filepath"
          fi
        done > "$FILES_TO_SIGN"
        
        echo ""
        echo "=== Files identified for signing ==="
        echo "Total files: $(wc -l < $FILES_TO_SIGN)"
        echo ""
        echo "Sample (first 20):"
        head -20 "$FILES_TO_SIGN"
        
      displayName: 'Identify binaries for signing'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish extracted payload'
      inputs:
        targetPath: '$(Pipeline.Workspace)/payload'
        artifactName: 'unsigned-binaries'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish file list'
      inputs:
        targetPath: '$(Pipeline.Workspace)/files-to-sign.txt'
        artifactName: 'files-to-sign-list'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish PKG metadata'
      inputs:
        targetPath: '$(Pipeline.Workspace)/pkg-extracted'
        artifactName: 'pkg-metadata'

- stage: SignBinaries
  displayName: 'Sign All Binaries with ESRP'
  dependsOn: DownloadAndExtract
  condition: and(succeeded(), eq(${{ parameters.OfficialBuild }}, true))
  jobs:
  - job: SignAllBinaries
    displayName: 'Sign binaries using ESRP'
    pool:
      vmImage: 'windows-2022'
    
    steps:
    - checkout: none
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download unsigned binaries'
      inputs:
        artifactName: 'unsigned-binaries'
        targetPath: '$(Pipeline.Workspace)/unsigned-binaries'
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download file list'
      inputs:
        artifactName: 'files-to-sign-list'
        targetPath: '$(Pipeline.Workspace)'
    
    - pwsh: |
        Write-Host "=== Preparing binaries for ESRP signing ==="
        
        $unsignedDir = "$(Pipeline.Workspace)/unsigned-binaries"
        $fileListPath = "$(Pipeline.Workspace)/files-to-sign.txt"
        $toBeSignedDir = "$(Pipeline.Workspace)/toBeSigned"
        
        # Create directory for files to be signed
        if (Test-Path $toBeSignedDir) {
          Remove-Item -Path $toBeSignedDir -Recurse -Force
        }
        New-Item -ItemType Directory -Path $toBeSignedDir -Force | Out-Null
        
        Write-Host ""
        Write-Host "Reading file list..."
        
        if (-not (Test-Path $fileListPath)) {
          Write-Error "File list not found: $fileListPath"
          exit 1
        }
        
        $filesToSign = Get-Content $fileListPath
        Write-Host "Files to sign: $($filesToSign.Count)"
        
        Write-Host ""
        Write-Host "Copying files to signing directory..."
        
        $copiedCount = 0
        foreach ($relPath in $filesToSign) {
          # Remove leading ./
          $relPath = $relPath.TrimStart('.', '/')
          
          $sourcePath = Join-Path $unsignedDir $relPath
          $targetPath = Join-Path $toBeSignedDir $relPath
          
          if (Test-Path $sourcePath) {
            $targetDir = Split-Path $targetPath -Parent
            if (-not (Test-Path $targetDir)) {
              New-Item -ItemType Directory -Path $targetDir -Force | Out-Null
            }
            
            Copy-Item -Path $sourcePath -Destination $targetPath -Force
            $copiedCount++
            
            if ($copiedCount -le 10) {
              Write-Host "  ✓ $relPath"
            }
          } else {
            Write-Warning "File not found: $sourcePath"
          }
        }
        
        Write-Host ""
        Write-Host "✅ Copied $copiedCount files to signing directory"
        Write-Host ""
        Write-Host "Signing directory size: $([math]::Round((Get-ChildItem $toBeSignedDir -Recurse | Measure-Object -Property Length -Sum).Sum / 1MB, 2)) MB"
        
      displayName: 'Prepare binaries for signing'
    
    - task: SFP.build-tasks.custom-build-task-1.EsrpCodeSigning@5
      displayName: 'ESRP - Sign macOS binaries'
      inputs:
        ConnectedServiceName: 'ame_esrp_connection'
        AppRegistrationClientId: '$(ESRPAppClientId)'
        AppRegistrationTenantId: '$(ESRPAppTenantId)'
        AuthAKVName: '$(ESRPKVName)'
        AuthCertName: '$(ESRPAuthCertName)'
        AuthSignCertName: '$(ESRPSignCertName)'
        FolderPath: '$(Pipeline.Workspace)/toBeSigned'
        Pattern: '**/*.dylib,**/*.so,**/python,**/python3,**/python3.*,**/az'
        signConfigType: 'inlineSignParams'
        inlineOperation: |
          [
            {
              "KeyCode": "CP-401337-Apple",
              "OperationCode": "MacAppDeveloperSign",
              "ToolName": "sign",
              "ToolVersion": "1.0",
              "Parameters": {
                "Hardening": "Enable",
                "OpusInfo": "http://www.microsoft.com",
                "BundleId": "$(BundleId)"
              }
            }
          ]
        SessionTimeout: '120'
        MaxConcurrency: '50'
        MaxRetryAttempts: '5'
    
    - pwsh: |
        Write-Host "=== Verifying signed binaries ==="
        
        $signedDir = "$(Pipeline.Workspace)/toBeSigned"
        
        Write-Host "Signed directory contents:"
        Get-ChildItem $signedDir -Recurse -File | Select-Object -First 20 | ForEach-Object {
          Write-Host "  - $($_.FullName.Replace($signedDir, '.'))"
        }
        
        Write-Host ""
        Write-Host "Total files signed: $(( Get-ChildItem $signedDir -Recurse -File | Measure-Object).Count)"
        
      displayName: 'Verify signed binaries'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish signed binaries'
      inputs:
        targetPath: '$(Pipeline.Workspace)/toBeSigned'
        artifactName: 'signed-binaries'

- stage: RepackageAndSignPkg
  displayName: 'Repackage PKG and Sign'
  dependsOn: SignBinaries
  condition: succeeded()
  jobs:
  - job: RepackagePkg
    displayName: 'Repackage PKG with signed binaries'
    pool:
      vmImage: 'macos-14'
    
    steps:
    - checkout: none
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download original payload'
      inputs:
        artifactName: 'unsigned-binaries'
        targetPath: '$(Pipeline.Workspace)/original-payload'
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download signed binaries'
      inputs:
        artifactName: 'signed-binaries'
        targetPath: '$(Pipeline.Workspace)/signed-binaries'
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download PKG metadata'
      inputs:
        artifactName: 'pkg-metadata'
        targetPath: '$(Pipeline.Workspace)/pkg-metadata'
    
    - bash: |
        set -e
        echo "=== Merging signed binaries back into payload ==="
        
        ORIGINAL_PAYLOAD="$(Pipeline.Workspace)/original-payload"
        SIGNED_BINARIES="$(Pipeline.Workspace)/signed-binaries"
        MERGED_PAYLOAD="$(Pipeline.Workspace)/merged-payload"
        
        # Copy original payload
        echo "Copying original payload..."
        cp -R "$ORIGINAL_PAYLOAD" "$MERGED_PAYLOAD"
        
        # Replace unsigned binaries with signed ones
        echo ""
        echo "Replacing unsigned binaries with signed versions..."
        
        cd "$SIGNED_BINARIES"
        find . -type f | while read signed_file; do
          target_file="$MERGED_PAYLOAD/$signed_file"
          
          if [ -f "$target_file" ]; then
            cp "$signed_file" "$target_file"
            echo "  ✓ Replaced: $signed_file"
          else
            echo "  ⚠ Not found in original: $signed_file"
          fi
        done
        
        echo ""
        echo "✅ Merged payload ready"
        echo "Size: $(du -sh $MERGED_PAYLOAD | cut -f1)"
        
      displayName: 'Merge signed binaries into payload'
    
    - bash: |
        set -e
        echo "=== Creating new Payload archive ==="
        
        MERGED_PAYLOAD="$(Pipeline.Workspace)/merged-payload"
        PKG_METADATA="$(Pipeline.Workspace)/pkg-metadata"
        OUTPUT_DIR="$(Pipeline.Workspace)/repacked-pkg"
        
        mkdir -p "$OUTPUT_DIR"
        
        # Find the component PKG directory
        COMPONENT_PKG=$(find "$PKG_METADATA" -name "*.pkg" -type d | grep -v "^\.$" | head -1)
        
        if [ -z "$COMPONENT_PKG" ]; then
          echo "❌ ERROR: No component PKG found!"
          exit 1
        fi
        
        echo "Component PKG: $COMPONENT_PKG"
        
        # Create new Payload from merged binaries
        echo ""
        echo "Creating Payload archive..."
        cd "$MERGED_PAYLOAD"
        
        # Create the Payload using cpio and gzip (same format as original)
        find . | cpio -o --format odc | gzip -c > "$OUTPUT_DIR/Payload"
        
        echo "✅ New Payload created: $(du -h $OUTPUT_DIR/Payload | cut -f1)"
        
        # Copy other PKG components (Bom, PackageInfo, Scripts if exists)
        echo ""
        echo "Copying PKG metadata files..."
        
        if [ -f "$COMPONENT_PKG/Bom" ]; then
          cp "$COMPONENT_PKG/Bom" "$OUTPUT_DIR/"
          echo "  ✓ Bom"
        fi
        
        if [ -f "$COMPONENT_PKG/PackageInfo" ]; then
          cp "$COMPONENT_PKG/PackageInfo" "$OUTPUT_DIR/"
          echo "  ✓ PackageInfo"
        fi
        
        if [ -d "$COMPONENT_PKG/Scripts" ]; then
          cp -R "$COMPONENT_PKG/Scripts" "$OUTPUT_DIR/"
          echo "  ✓ Scripts"
        fi
        
        # Copy Distribution file
        if [ -f "$PKG_METADATA/Distribution" ]; then
          cp "$PKG_METADATA/Distribution" "$(Pipeline.Workspace)/"
          echo "  ✓ Distribution"
        fi
        
        echo ""
        echo "✅ PKG components ready for repacking"
        
      displayName: 'Create new Payload archive'
    
    - bash: |
        set -e
        echo "=== Building new PKG with signed binaries ==="
        
        REPACKED_DIR="$(Pipeline.Workspace)/repacked-pkg"
        DISTRIBUTION="$(Pipeline.Workspace)/Distribution"
        OUTPUT_PKG="$(Pipeline.Workspace)/azure-cli-$(AzureCliVersion)-macos-arm64-signed-binaries.pkg"
        
        # Create component package
        COMPONENT_PKG="$(Pipeline.Workspace)/azure-cli-component.pkg"
        
        echo "Creating component PKG from repacked contents..."
        pkgbuild --root "$REPACKED_DIR" \
                 --identifier "$(BundleId)" \
                 --version "$(AzureCliVersion)" \
                 --install-location "/usr/local/az" \
                 "$COMPONENT_PKG"
        
        echo "✅ Component PKG created: $(du -h $COMPONENT_PKG | cut -f1)"
        
        # Create product archive (distribution package)
        echo ""
        echo "Creating distribution PKG..."
        
        if [ -f "$DISTRIBUTION" ]; then
          productbuild --distribution "$DISTRIBUTION" \
                       --package-path "$(Pipeline.Workspace)" \
                       "$OUTPUT_PKG"
        else
          # If no Distribution file, create simple product package
          productbuild --component "$COMPONENT_PKG" /usr/local/az \
                       --identifier "$(BundleId)" \
                       --version "$(AzureCliVersion)" \
                       "$OUTPUT_PKG"
        fi
        
        echo ""
        echo "✅ PKG created: $OUTPUT_PKG"
        echo "Size: $(du -h $OUTPUT_PKG | cut -f1)"
        
        # Verify PKG structure
        echo ""
        echo "=== PKG Verification ==="
        pkgutil --check-signature "$OUTPUT_PKG" || echo "Not yet signed (expected)"
        
      displayName: 'Build new PKG with signed binaries'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish PKG with signed binaries'
      inputs:
        targetPath: '$(Pipeline.Workspace)/azure-cli-$(AzureCliVersion)-macos-arm64-signed-binaries.pkg'
        artifactName: 'pkg-with-signed-binaries'

- stage: SignPkgItself
  displayName: 'Sign the PKG Archive'
  dependsOn: RepackageAndSignPkg
  condition: and(succeeded(), eq(${{ parameters.OfficialBuild }}, true))
  jobs:
  - job: SignPkg
    displayName: 'Sign PKG with ESRP'
    pool:
      vmImage: 'windows-2022'
    
    steps:
    - checkout: none
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download PKG with signed binaries'
      inputs:
        artifactName: 'pkg-with-signed-binaries'
        targetPath: '$(Pipeline.Workspace)/pkg-to-sign'
    
    - pwsh: |
        Write-Host "=== Preparing PKG for ESRP signing ==="
        
        $pkgFile = Get-ChildItem -Path "$(Pipeline.Workspace)/pkg-to-sign" -Filter "*.pkg" -File | Select-Object -First 1
        
        if (-not $pkgFile) {
          Write-Error "❌ PKG file not found"
          exit 1
        }
        
        Write-Host "✅ Found PKG: $($pkgFile.Name)"
        Write-Host "Size: $([math]::Round($pkgFile.Length / 1MB, 2)) MB"
        
        # Compress to ZIP for ESRP
        $zipPath = "$(Pipeline.Workspace)/$($pkgFile.BaseName).zip"
        
        Write-Host ""
        Write-Host "Compressing to ZIP for ESRP..."
        Compress-Archive -Path $pkgFile.FullName -DestinationPath $zipPath -Force
        
        if (Test-Path $zipPath) {
          Write-Host "✅ ZIP created: $([math]::Round((Get-Item $zipPath).Length / 1MB, 2)) MB"
        } else {
          Write-Error "❌ Failed to create ZIP"
          exit 1
        }
        
      displayName: 'Compress PKG for ESRP signing'
    
    - task: SFP.build-tasks.custom-build-task-1.EsrpCodeSigning@5
      displayName: 'ESRP - Sign PKG'
      inputs:
        ConnectedServiceName: 'ame_esrp_connection'
        AppRegistrationClientId: '$(ESRPAppClientId)'
        AppRegistrationTenantId: '$(ESRPAppTenantId)'
        AuthAKVName: '$(ESRPKVName)'
        AuthCertName: '$(ESRPAuthCertName)'
        AuthSignCertName: '$(ESRPSignCertName)'
        FolderPath: '$(Pipeline.Workspace)'
        Pattern: 'azure-cli-*-macos-arm64-signed-binaries.zip'
        signConfigType: 'inlineSignParams'
        inlineOperation: |
          [
            {
              "KeyCode": "CP-401337-Apple",
              "OperationCode": "MacAppDeveloperSign",
              "ToolName": "sign",
              "ToolVersion": "1.0",
              "Parameters": {
                "Hardening": "Enable",
                "OpusInfo": "http://www.microsoft.com",
                "BundleId": "$(BundleId)"
              }
            }
          ]
        SessionTimeout: '60'
        MaxConcurrency: '50'
        MaxRetryAttempts: '5'
    
    - pwsh: |
        Write-Host "=== Extracting Signed PKG ==="
        
        $signedZip = Get-ChildItem -Path "$(Pipeline.Workspace)" -Filter "*signed-binaries.zip" -File | Select-Object -First 1
        
        if (-not $signedZip) {
          Write-Error "❌ Signed ZIP not found"
          exit 1
        }
        
        Write-Host "Found signed ZIP: $($signedZip.Name)"
        
        $extractPath = "$(Pipeline.Workspace)/signed-pkg"
        if (Test-Path $extractPath) {
          Remove-Item -Path $extractPath -Recurse -Force
        }
        New-Item -ItemType Directory -Path $extractPath -Force | Out-Null
        
        Expand-Archive -Path $signedZip.FullName -DestinationPath $extractPath -Force
        
        $signedPkg = Get-ChildItem -Path $extractPath -Filter "*.pkg" -File | Select-Object -First 1
        
        if (-not $signedPkg) {
          Write-Error "❌ No PKG found in ZIP"
          exit 1
        }
        
        # Rename to final name
        $finalName = "azure-cli-$(AzureCliVersion)-macos-arm64-fully-signed.pkg"
        $finalPath = "$(Build.ArtifactStagingDirectory)/$finalName"
        
        if (-not (Test-Path "$(Build.ArtifactStagingDirectory)")) {
          New-Item -ItemType Directory -Path "$(Build.ArtifactStagingDirectory)" -Force | Out-Null
        }
        
        Copy-Item -Path $signedPkg.FullName -Destination $finalPath -Force
        
        Write-Host ""
        Write-Host "✅ Fully signed PKG ready: $finalName"
        Write-Host "Size: $([math]::Round((Get-Item $finalPath).Length / 1MB, 2)) MB"
        Write-Host "Path: $finalPath"
        
        # Generate SHA256
        $sha256Path = "$finalPath.sha256"
        $hash = (Get-FileHash -Path $finalPath -Algorithm SHA256).Hash
        Set-Content -Path $sha256Path -Value $hash
        
        Write-Host ""
        Write-Host "SHA256: $hash"
        
      displayName: 'Extract and rename signed PKG'
    
    - task: PublishPipelineArtifact@1
      displayName: 'Publish fully signed PKG'
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)'
        artifactName: 'fully-signed-macos-pkg'

- stage: VerifySignatures
  displayName: 'Verify All Signatures'
  dependsOn: SignPkgItself
  condition: succeeded()
  jobs:
  - job: VerifyOnMac
    displayName: 'Verify signatures on macOS'
    pool:
      vmImage: 'macos-14'
    
    steps:
    - checkout: none
    
    - task: DownloadPipelineArtifact@2
      displayName: 'Download fully signed PKG'
      inputs:
        artifactName: 'fully-signed-macos-pkg'
        targetPath: '$(Pipeline.Workspace)/signed-pkg'
    
    - bash: |
        set -e
        echo "=== Verifying PKG Signature ==="
        
        PKG_FILE=$(find $(Pipeline.Workspace)/signed-pkg -name "*.pkg" -type f | head -1)
        
        if [ -z "$PKG_FILE" ]; then
          echo "❌ PKG file not found!"
          exit 1
        fi
        
        echo "PKG: $PKG_FILE"
        echo ""
        
        # Check PKG signature
        echo "=== PKG Signature Check ==="
        pkgutil --check-signature "$PKG_FILE"
        
        echo ""
        echo "=== Extracting to verify binary signatures ==="
        
        VERIFY_DIR="$(Pipeline.Workspace)/verify-extract"
        mkdir -p "$VERIFY_DIR"
        cd "$VERIFY_DIR"
        
        # Extract PKG
        xar -xf "$PKG_FILE"
        
        COMPONENT_PKG=$(find . -name "*.pkg" -type d | grep -v "^\.$" | head -1)
        
        if [ ! -z "$COMPONENT_PKG" ]; then
          # Extract payload
          PAYLOAD_DIR="$VERIFY_DIR/payload"
          mkdir -p "$PAYLOAD_DIR"
          cd "$PAYLOAD_DIR"
          
          cat "$VERIFY_DIR/$COMPONENT_PKG/Payload" | gunzip -dc | cpio -i 2>/dev/null
          
          echo ""
          echo "=== Checking Binary Signatures (sample) ==="
          
          # Check a few binaries
          find . -type f \( -name "*.dylib" -o -name "*.so" -o -name "python*" -o -name "az" \) | head -5 | while read binary; do
            if [ -f "$binary" ]; then
              echo ""
              echo "Checking: $binary"
              codesign -dv "$binary" 2>&1 || echo "Not signed or not a Mach-O binary"
            fi
          done
        fi
        
        echo ""
        echo "✅ Verification complete"
        
      displayName: 'Verify PKG and binary signatures'
      continueOnError: true
