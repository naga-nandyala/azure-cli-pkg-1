# Azure CLI - macOS Tarball Build Pipeline
#
# Build CLI tarball using Homebrew Python (no bundled Python)
#
# This pipeline creates a lightweight tar.gz that:
# - Does NOT bundle Python runtime (~60 MB savings)
# - Relies on Homebrew python@3.13 as a runtime dependency
# - Contains only Azure CLI packages and native extensions (.so files)
# - Uses portable launcher script supporting multiple Python sources
# - Builds BOTH ARM64 and x86_64 architectures in parallel
#
# Stages:
# 1. Build Azure CLI packages for both ARM64 and x86_64 (parallel)
# 2. Analyze and Verify on corresponding architecture runners
# 3. Summary
#
# Output Artifacts:
# - cli-build-unsigned-v3-arm64
# - cli-build-unsigned-v3-x86_64
#
# The output is an UNSIGNED tar.gz that needs to be signed and notarized
# in a separate pipeline (macos-tarball-sign-notarize-v3.yml)

trigger: none

parameters:
  - name: PythonVersion
    displayName: 'Python Version (Homebrew)'
    type: string
    default: '3.13'

resources:
  repositories:
  - repository: selfRepo
    type: github
    endpoint: 'github.com_naga-nandyala'
    name: naga-nandyala/azure-cli-pkg-1
    ref: main

variables:
  - template: templates/variables.yml
  # Disable auto-injection tasks
  - name: Codeql.Enabled
    value: false
  - name: Codeql.SkipTaskAutoInjection
    value: true
  - name: CodeQL.enabled
    value: false
  - name: runCodesignValidationInjection
    value: false
  - name: DOTNET_CLI_TELEMETRY_OPTOUT
    value: 1
  - name: DOTNET_NOLOGO
    value: 1
  - name: NugetSecurityAnalysisWarningLevel
    value: none
  - name: skipNugetSecurityAnalysis
    value: true

name: tarball-build-v3-$(Build.BuildId)

stages:
# ============================================================================
# STAGE 1: BUILD AZURE CLI (BOTH ARCHITECTURES IN PARALLEL)
# ============================================================================
- stage: BuildAzureCli
  displayName: 'Stage 1: Build Azure CLI'
  jobs:
  - job: BuildCli
    displayName: 'Build'
    strategy:
      matrix:
        ARM64:
          Architecture: 'arm64'
          vmImageName: ${{ variables.macos_arm64_pool }}
        Intel:
          Architecture: 'x86_64'
          vmImageName: ${{ variables.macos_intel_pool }}
    pool:
      vmImage: $(vmImageName)
    timeoutInMinutes: 60
    
    steps:
    - checkout: self
      fetchDepth: 1
    
    # Install Homebrew Python
    - bash: |
        set -e
        
        echo "=== Installing Homebrew Python ${{ parameters.PythonVersion }} ==="
        echo "Architecture: $(Architecture)"
        echo "VM Image: $(vmImageName)"
        brew install python@${{ parameters.PythonVersion }}
        
        # Get Python path
        PYTHON_PATH=$(brew --prefix python@${{ parameters.PythonVersion }})/libexec/bin/python3
        
        echo "Python path: $PYTHON_PATH"
        $PYTHON_PATH --version
        
        echo "##vso[task.setvariable variable=PythonPath]$PYTHON_PATH"
      displayName: 'Install Homebrew Python'
    
    # Build Azure CLI
    - bash: |
        set -e
        
        PYTHON="$(PythonPath)"
        ARCH="$(Architecture)"
        
        echo "=== Building Azure CLI with Homebrew Python ==="
        echo "Python: $PYTHON"
        echo "Architecture: $ARCH"
        $PYTHON --version
        
        # Run the v3 build script
        echo ""
        echo "=== Running build_binary_tar_gz_v3.py ==="
        $PYTHON scripts/release/macos/build_binary_tar_gz_v3.py \
          --platform-tag macos-$ARCH \
          --output-dir dist/binary_tar_gz
        
        # List output
        echo ""
        echo "=== Build Output ==="
        ls -lh dist/binary_tar_gz/
        
        # Copy to staging
        mkdir -p $(Build.ArtifactStagingDirectory)/cli-build
        cp dist/binary_tar_gz/*.tar.gz $(Build.ArtifactStagingDirectory)/cli-build/
        cp dist/binary_tar_gz/*.sha256 $(Build.ArtifactStagingDirectory)/cli-build/
        
        echo ""
        echo "=== Staged Artifacts ==="
        ls -lh $(Build.ArtifactStagingDirectory)/cli-build/
        
      displayName: 'Build Azure CLI Tarball'
      name: BuildStep
    
    - publish: $(Build.ArtifactStagingDirectory)/cli-build
      artifact: 'cli-build-unsigned-v3-$(Architecture)'
      displayName: 'Publish Unsigned CLI Build ($(Architecture))'

# ============================================================================
# STAGE 2: ANALYZE AND VERIFY (BOTH ARCHITECTURES IN PARALLEL)
# ============================================================================
- stage: AnalyzeAndVerify
  displayName: 'Stage 2: Analyze and Verify'
  dependsOn: BuildAzureCli
  jobs:
  - job: AnalyzeTarball
    displayName: 'Verify'
    strategy:
      matrix:
        ARM64:
          Architecture: 'arm64'
          vmImageName: 'macos-15-arm64'
        Intel:
          Architecture: 'x86_64'
          vmImageName: 'macos-15'
    pool:
      vmImage: $(vmImageName)
    steps:
    - checkout: none
    
    - download: current
      artifact: 'cli-build-unsigned-v3-$(Architecture)'
      displayName: 'Download CLI Build ($(Architecture))'
    
    - bash: |
        set -e
        
        ARCH="$(Architecture)"
        TARBALL=$(find $(Pipeline.Workspace)/cli-build-unsigned-v3-$ARCH -name "azure-cli-*-macos-$ARCH-nopython.tar.gz" | sort | tail -1)
        EXTRACT_DIR=$(mktemp -d)
        
        echo "=== Tarball Analysis (No Bundled Python) ==="
        echo "Architecture: $ARCH"
        echo "Tarball: $(basename "$TARBALL")"
        TARBALL_SIZE=$(du -h "$TARBALL" | cut -f1)
        echo "Size: $TARBALL_SIZE"
        echo ""
        
        tar -xzf "$TARBALL" -C "$EXTRACT_DIR"
        
        echo "=== Directory Structure ==="
        find "$EXTRACT_DIR" -maxdepth 3 -type d | head -20
        echo ""
        
        echo "=== Native Extensions (.so files) to Sign ==="
        SO_FILES=$(find "$EXTRACT_DIR" -name "*.so" -type f)
        SO_COUNT=$(echo "$SO_FILES" | grep -c . || echo 0)
        echo "Total .so files: $SO_COUNT"
        echo ""
        echo "Files:"
        echo "$SO_FILES" | while read f; do
          if [ -n "$f" ]; then
            SIZE=$(du -h "$f" | cut -f1)
            NAME=$(basename "$f")
            echo "  $NAME ($SIZE)"
          fi
        done
        
        echo ""
        echo "=== Dylib Files ==="
        DYLIB_FILES=$(find "$EXTRACT_DIR" -name "*.dylib" -type f)
        DYLIB_COUNT=$(echo "$DYLIB_FILES" | grep -c . || echo 0)
        echo "Total .dylib files: $DYLIB_COUNT"
        
        echo ""
        echo "=== Total Binaries to Sign ==="
        TOTAL=$((SO_COUNT + DYLIB_COUNT))
        echo "Total: $TOTAL"
        echo "##vso[task.setvariable variable=TarballSize;isOutput=true]$TARBALL_SIZE"
        
        echo ""
        echo "=== Launcher Script ==="
        cat "$EXTRACT_DIR/libexec/bin/az" | head -30
        
        echo ""
        echo "=== Tarball Contents Summary ==="
        echo "Extracted size: $(du -sh "$EXTRACT_DIR" | cut -f1)"
        echo "File count: $(find "$EXTRACT_DIR" -type f | wc -l)"
        
        rm -rf "$EXTRACT_DIR"
      displayName: 'Analyze Tarball ($(Architecture))'
      name: AnalyzeStep
    
    # Verify Azure CLI works on the correct architecture
    - bash: |
        set -e
        
        ARCH="$(Architecture)"
        TARBALL=$(find $(Pipeline.Workspace)/cli-build-unsigned-v3-$ARCH -name "azure-cli-*-macos-$ARCH-nopython.tar.gz" | sort | tail -1)
        EXTRACT_DIR=$(mktemp -d)
        
        echo "=== Verifying Azure CLI on $ARCH ==="
        tar -xzf "$TARBALL" -C "$EXTRACT_DIR"
        
        # Install Homebrew Python if not present
        if ! brew list python@${{ parameters.PythonVersion }} &>/dev/null; then
          brew install python@${{ parameters.PythonVersion }}
        fi

        AZ_PYTHON="$(brew --prefix python@${{ parameters.PythonVersion }})/libexec/bin/python3"
        export AZ_PYTHON
        
        # Verify we're running on the correct architecture
        echo "System architecture: $(uname -m)"
        echo "Expected: $ARCH"
        
        # Test CLI
        export AZ_DEBUG=1
        "$EXTRACT_DIR/bin/az" version
        
        echo ""
        echo "Azure CLI works correctly on $ARCH"
        
        rm -rf "$EXTRACT_DIR"
      displayName: 'Verify CLI Works ($(Architecture))'

# ============================================================================
# STAGE 3: SUMMARY
# ============================================================================
- stage: Summary
  displayName: 'Stage 3: Build Summary'
  dependsOn:
  - BuildAzureCli
  - AnalyzeAndVerify
  condition: always()
  variables:
    TarballSizeARM64: $[ stageDependencies.AnalyzeAndVerify.AnalyzeTarball.ARM64.outputs['AnalyzeStep.TarballSize'] ]
    TarballSizeIntel: $[ stageDependencies.AnalyzeAndVerify.AnalyzeTarball.Intel.outputs['AnalyzeStep.TarballSize'] ]
  jobs:
  - job: PrintSummary
    displayName: 'Print Build Summary'
    pool:
      vmImage: 'ubuntu-latest'
    
    steps:
    - checkout: none
    
    - bash: |
        echo "╔══════════════════════════════════════════════════════════════════════╗"
        echo "║          AZURE CLI TARBALL BUILD SUMMARY                             ║"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ Architectures:  ARM64 (Apple Silicon) + x86_64 (Intel)"
        echo "║ Python:         Homebrew ${{ parameters.PythonVersion }} (not bundled)"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ TARBALL SIZES                                                        ║"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ ARM64 tarball size:    $(TarballSizeARM64)"
        echo "║ Intel tarball size:    $(TarballSizeIntel)"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ ARTIFACTS                                                            ║"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ • cli-build-unsigned-v3-arm64                                        ║"
        echo "║   └── azure-cli-<version>-macos-arm64-nopython.tar.gz"
        echo "║ • cli-build-unsigned-v3-x86_64                                       ║"
        echo "║   └── azure-cli-<version>-macos-x86_64-nopython.tar.gz"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ NEXT STEP                                                            ║"
        echo "╠══════════════════════════════════════════════════════════════════════╣"
        echo "║ Run macos-tarball-sign-notarize-v3.yml with:                         ║"
        echo "║   TarballBuildId: $(Build.BuildId)                                   ║"
        echo "║   Architecture: arm64 OR x86_64                                      ║"
        echo "╚══════════════════════════════════════════════════════════════════════╝"
      displayName: 'Print Summary'
